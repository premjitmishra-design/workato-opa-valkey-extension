# Pin the JDK Gradle builds this project with. Adjust if your JDK 17 lives elsewhere
# (e.g. after `brew install openjdk@17`, this is the standard Homebrew Cellar path on Apple Silicon).
org.gradle.java.home=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
